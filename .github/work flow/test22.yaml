name: file operation in github

on: push

jobs:
  files:
      runs-on:ubuntu-latest

      steps:
        - name:create a file
          run:echo "welcome to vijay youtube channel" >devlops.txt


          - name:display the content of the file
            run: cat devops.txt

           - name:display the date
             run:date

             - name:display the file and dire
               run:ls-lrth

               - name:display the present location
                 run:pwd  

